void myPitchBend(byte channel, int bend) {
  // float bendF = bend;
  // float bendRange = 1.0;
  // bendF = bendF / 8192;
  // bendF = bendF * bendRange;
  // bendF = bendF / 12;
  // float bendFactor = pow(2, bendF);
}